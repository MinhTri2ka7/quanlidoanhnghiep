spring:
  application:
    name: quanlydn-backend

  profiles:
    active: prod

  mail:
    host: smtp.gmail.com
    port: 587
    username: "minhtri1012007@gmail.com"
    password: "wvxn xetz tmib nfur"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

logging:
  level:
    root: INFO
    com.quanlydn: DEBUG

rate-limit:
  enabled: true
  limit: 100
  duration-seconds: 60


